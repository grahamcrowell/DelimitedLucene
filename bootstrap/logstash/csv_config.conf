input {
  file {
    path => "/Users/gcrowell/TreasureData/PriceData/AA*"
    start_position => "beginning"
    sincedb_path => "/dev/null"
  }
}

filter {
  csv {
    id=>"fuckme"
    separator=>","
    autodetect_column_names=>true
  }
}

output {
  elasticsearch { 
    hosts => ["localhost:9200"]
    index => "stocks"
  }
  # stdout { codec => rubydebug }
}